# Fichero de configuración para la conexión a la base de datos MySQL

# URL de conexión JDBC para MySQL
# jdbc:mysql://<host>:<port>/<database_name>
db.url=jdbc:mysql://localhost:3306/proyecto_orm?useSSL=false&serverTimezone=UTC
# Usuario de la base de datos
db.user=root

# Contraseña del usuario
## Password REMOVED for security. Do NOT commit secrets to the repository.
## Set the password at runtime using the environment variable `DB_PASSWORD`.
db.password=Ssbjmc0602!

# Driver JDBC de MySQL (Opcional a partir de JDBC 4.0, pero buena práctica incluirlo)
db.driver=com.mysql.cj.jdbc.Driver